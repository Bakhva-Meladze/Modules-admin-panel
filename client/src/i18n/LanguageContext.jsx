/**
 * Re-export only — keeps old import paths working if Vite cache still references this file.
 * Prefer: `import { LanguageContext } from "./languageContext.js"`
 * Provider: `import { LanguageProvider } from "./LanguageProvider.jsx"`
 */
export { LanguageContext, translate } from "./languageContext.js";
